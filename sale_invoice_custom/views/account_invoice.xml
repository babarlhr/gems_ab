<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- <record id="invoice_form_inherited_view" model="ir.ui.view">
        <field name="name">account.invoice.form.inherited.view</field>
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_form" />
        <field name="arch" type="xml"> -->
            <!-- <field name="invoice_line_tax_ids" position="after">
                <field name="vat_percent"/>
                <field name="total_vat_lines" />
            </field> -->
            <!-- <field name="price_subtotal" position="after">
                <field name="total_with_vat" />
            </field> -->
            <!-- <field name="amount_tax" position="after">
                <field name="vat_amount" />
            </field> -->
        <!-- </field> -->
    <!-- </record> -->
    
</odoo>